<h2><%= @post.title %></h2>
<p><%= @post.content %></p>
<small><%= @post.created_at %></small>
</br>
</br>

<%= link_to 'Edit',edit_post_path(@post), :class => 'PostsController' %>

<%= link_to 'Delete', post_path(@post), method: :delete, data:{ confirm: 'Are you sure ?'} %>

<%= link_to 'Back',posts_path %>